<% if @vote.valid? %>
	$("#post_<%= @post.id%>_votes").html("<%= @post.total_votes %> <%= 'vote'.pluralize(@post.total_votes) %>");
<% else %>
	alert("You have already voted on this post.");
<% end %>